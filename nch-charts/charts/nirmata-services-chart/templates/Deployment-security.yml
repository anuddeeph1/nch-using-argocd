{{- if .Values.users.enabled }}
---
kind: "Deployment"
apiVersion: "apps/v1"
metadata:
  name: "security"
  namespace: {{ include "helmchart.namespace" . | quote }}
  labels:
    nirmata.io/deployment.name: "security"
    nirmata.io/service.name: "security"
    nirmata.io/type: "app"
spec:
  progressDeadlineSeconds: 2147483647
  replicas: {{ .Values.security.replicas }}
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      nirmata.io/service.name: "security"
    matchExpressions: []
  strategy:
    type: "RollingUpdate"
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      namespace: {{ include "helmchart.namespace" . | quote }}
      labels:
        nirmata.io/deployment.name: "security"
        nirmata.io/service.name: "security"
        nirmata.io/type: "app"
        nirmata.io/force.rolling.update: "2"
        {{- range .Values.security.labels }}
        {{- range $key, $value := . }}
        {{ $key }}: {{ $value }}
        {{- end }}
        {{- end }}
    spec:
      serviceAccountName: "security-service"
      automountServiceAccountToken: false
      restartPolicy: "Always"
      terminationGracePeriodSeconds: 30
      nodeSelector: {}
      schedulerName: "default-scheduler"
      priorityClassName: "nirmata-app-critical"
      dnsPolicy: "ClusterFirst"
      imagePullSecrets:
      - name: {{ .Values.global.imagePullSecrets.name | default "registry-secret" }}
      containers:
      - name: "security"
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
{{- if ( ne .Values.global.provider "openshift" ) }}
          seccompProfile:
            type: RuntimeDefault
{{- end }}
          capabilities:
            drop:
              - ALL
{{- if .Values.security.image.tag }}
        image: "{{.Values.global.common.registry }}/{{ .Values.security.image.name }}:{{ .Values.security.image.tag }}"
{{- else }}
        image: "{{.Values.global.common.registry }}/{{ .Values.security.image.name }}:{{ .Values.global.common.version }}"
{{- end }}
        command: []
        args: []
        terminationMessagePath: "/dev/termination-log"
        terminationMessagePolicy: "File"
        imagePullPolicy: {{ .Values.security.image.pullPolicy }}
        ports:
        - name: "https"
          containerPort: 8443
          protocol: "TCP"
        env:
        - name: "JAVA_TOOL_OPTIONS"
{{- if .Values.security.env.JAVA_TOOL_OPTIONS.value }}
          value: {{ .Values.security.env.JAVA_TOOL_OPTIONS.value }}
{{- else }}
          value: {{ .Values.global.env.JAVA_TOOL_OPTIONS.value }}
{{- end }}
        - name: "JAVA_OPTS"
{{- if .Values.security.env.JAVA_OPTS.value }}
          value: {{ .Values.security.env.JAVA_OPTS.value }}
{{- else }}
          value: {{ .Values.global.env.JAVA_OPTS.value }}
{{- end }}
        - name: "nirmata.environment.name"
          valueFrom:
            fieldRef:
              apiVersion: "v1"
              fieldPath: "metadata.namespace"
        - name: "eureka.registration.enabled"
          value: "false"
        - name: "nirmata.service.dnsclient"
          value: "true"
        - name: "nirmata.zookeeper.address"
          value: "zookeeper:2181"
        - name: "nirmata.kafka.address"
          value: "kafka:9092"
        - name: "nirmata.product.npmonly"
          valueFrom:
            configMapKeyRef:
              key: isNchOnly
              name: nirmata-config
        - name: "nirmata.product.nchonly"
          valueFrom:
            configMapKeyRef:
              key: isNchOnly
              name: nirmata-config
        - name: "service.logs.level"
          value: {{ .Values.security.logLevel | quote }}
        - name: "nirmata.gateway.address"
          valueFrom:
            configMapKeyRef:
              name: "nirmata-config"
              key: "nirmata.url"
        - name: nirmata.product.name
          valueFrom:
            configMapKeyRef:
              key: "productName"
              name: "nirmata-config"     
        - name: "nirmata.product.installtype"
          valueFrom:
            configMapKeyRef:
              name: "nirmata-config"
              key: "productName"
        - name: "nirmata.product.tenancymodel"
          valueFrom:
            configMapKeyRef:
              name: "nirmata-config"
              key: "tenancyModel"      
        - name: "HTTP_PROXY"
          valueFrom:
            configMapKeyRef:
              name: "nirmata-config"
              key: "HTTP_PROXY"
        - name: "HTTPS_PROXY"
          valueFrom:
            configMapKeyRef:
              name: "nirmata-config"
              key: "HTTPS_PROXY"
        - name: "NO_PROXY"
          valueFrom:
            configMapKeyRef:
              name: "nirmata-config"
              key: "NO_PROXY"
        - name: "http_proxy"
          valueFrom:
            configMapKeyRef:
              name: "nirmata-config"
              key: "http_proxy"
        - name: "https_proxy"
          valueFrom:
            configMapKeyRef:
              name: "nirmata-config"
              key: "https_proxy"
        - name: "no_proxy"
          valueFrom:
            configMapKeyRef:
              name: "nirmata-config"
              key: "no_proxy"
        livenessProbe:
{{ toYaml .Values.security.livenessProbe | indent 10 }}
        readinessProbe:
{{ toYaml .Values.security.readinessProbe | indent 10 }}
        resources:
{{ toYaml .Values.security.resources | indent 10 }}
      securityContext:
        supplementalGroups: []
{{- if ( gt ( int .Values.security.replicas ) 1 ) }}
      affinity:
{{ toYaml .Values.security.affinity | indent 8 }}
{{- end}}
      tolerations:
{{ toYaml .Values.security.tolerations | indent 8 }}
{{- end }}
