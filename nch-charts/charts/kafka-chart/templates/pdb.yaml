---
apiVersion: "policy/v1"
kind: "PodDisruptionBudget"
metadata:
  name: "kafka-pdb"
  namespace: {{ include "helmchart.namespace" . | quote }}
  labels: {}
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: "kafka"
    matchExpressions: []

