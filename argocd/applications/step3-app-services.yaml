apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nch-step3-app-services
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: nch-charts
  source:
    repoURL: https://github.com/your-org/nch-charts  # Replace with your repo URL
    targetRevision: HEAD
    path: nch-charts
    helm:
      valueFiles:
        - ../config/values/base.yaml
        - ../config/values/environments/dev.yaml  # Change to prod.yaml for production
        - ../config/values/steps/app-services.yaml
      parameters:
        - name: prereq.enabled
          value: "false"
        - name: base.enabled
          value: "false"
        - name: others.enabled
          value: "true"
        - name: postreq.enabled
          value: "false"
  destination:
    server: https://kubernetes.default.svc
    namespace: nch-dev1  # Change to nch-pe for production
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 15m
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
        - /spec/template/metadata/annotations/deployment.kubernetes.io~1revision
    - group: v1
      kind: Secret
      jsonPointers:
        - /data
